# Memory Recall Temp Workspace Evidence Execution

Status: `MEMORY_RECALL_TEMP_WORKSPACE_EVIDENCE_COMPLETED_NOT_READY`
Date: 2026-05-22
Scope: temp-workspace bounded recall evidence execution
Baseline: `17a718532450c9c080c8fa8bf8fb5ec276240119`
Controlling state: `RC_NOT_READY_BLOCKED`

## Purpose

This packet executes the CM-0757 temp workspace recall evidence plan.

The evidence layer sits between CM-0755 fixture-only in-memory evidence and any future true live real-store `search_memory`. It uses only an isolated temp workspace, four synthetic seed records, exactly four bounded queries, sanitized evidence output, and cleanup verification.

This packet does not read real memory content, does not read `.jsonl` audit or durable memory content, does not call providers, does not write durable memory or audit state, and does not claim memory recall reliability.

## Execution Surface

Targeted test:

```text
node --test tests\memory-recall-temp-workspace-evidence.test.js
```

Targeted result:

```text
pass: 1/1
```

Changed execution file:

```text
tests/memory-recall-temp-workspace-evidence.test.js
```

The test is temp-workspace-only. It does not call MCP `search_memory`, does not use the real runtime store, and does not read real diary, audit, shadow-store, or user-owned memory content.

## Temp Workspace Root Evidence

Root policy implemented:

- Parent root: `<repo>/tmp/memory-recall-temp-workspace-evidence`.
- Run root: `<repo>/tmp/memory-recall-temp-workspace-evidence/CM-0758-<run-id>`.
- The test resolves the parent and run root before use.
- The run root must start with the resolved temp parent path.
- The test creates exactly one run-specific temp root.
- The run root contains only synthetic seed files generated by the test.
- The seed files use `.json`, not `.jsonl`.
- The test fails closed if the run root is outside the expected temp parent.

Cleanup evidence:

- Cleanup target is the resolved run-specific temp root only.
- Cleanup occurs after the four bounded queries.
- Cleanup uses the resolved run root after the parent containment assertion.
- The test verifies the run root no longer exists after cleanup.
- Empty parent directory visibility in Git is not evidence and is not committed.

## Synthetic Seed Evidence

The test writes exactly four synthetic seed records to the temp root:

| memoryId | target | folder | role |
|---|---|---|---|
| `temp-recall-expected-current` | `process` | `alpha` | expected current result |
| `temp-recall-expected-older` | `process` | `alpha` | expected older result |
| `temp-recall-irrelevant-same-folder` | `process` | `alpha` | irrelevant same-folder result |
| `temp-recall-irrelevant-other-folder` | `process` | `beta` | irrelevant other-folder result |

Seed boundaries:

- Seeds are synthetic and non-sensitive.
- Seeds are generated inside the temp root only.
- Seeds are not copied from real memory.
- Seeds are not copied from `.jsonl` audit or durable memory content.
- Seeds exercise expected match, irrelevant suppression, freshness ordering, and folder filtering.

## Query Evidence

Exact query count:

```text
queryCount = 4
```

Executed bounded query set:

| query # | purpose | query / scope | accepted evidence |
|---:|---|---|---|
| 1 | expected result | `temp alpha bounded recall` | returns `temp-recall-expected-current` first and `temp-recall-expected-older` second |
| 2 | irrelevant suppression | `temp alpha bounded recall` | excludes `temp-recall-irrelevant-same-folder` and `temp-recall-irrelevant-other-folder` from accepted results |
| 3 | folder behavior | `temp alpha bounded recall`, folder scope `alpha` | excludes beta-folder record `temp-recall-irrelevant-other-folder` |
| 4 | timeout/error boundary | `temp alpha bounded recall timeout`, bounded timeout `5 ms` | returns `SEARCH_MEMORY_TIMEOUT` with JSON-RPC code `-32002` |

No additional query is executed by the test.

## Result Criteria

Expected result:

- `temp-recall-expected-current` is returned.
- It is the primary result.
- `temp-recall-expected-older` may appear after the current result.

Irrelevant suppression:

- `temp-recall-irrelevant-same-folder` is not accepted.
- `temp-recall-irrelevant-other-folder` is not accepted.
- No non-synthetic result appears.

Freshness and folder behavior:

- Current synthetic expected record sorts before older synthetic expected record.
- Alpha folder-scoped query excludes beta folder record.
- This is temp workspace behavior only, not real-store freshness proof and not VCP full parity proof.

Timeout/error boundary:

- Timeout returns `SEARCH_MEMORY_TIMEOUT`.
- JSON-RPC code remains `-32002`.
- Timeout evidence remains bounded and sanitized.
- Timeout side-effect counters remain zero.

## Sanitized Evidence Object

The test constructs a sanitized evidence object equivalent to:

```json
{
  "taskId": "MEMORY_RECALL_TEMP_WORKSPACE_EVIDENCE_EXECUTION",
  "baseline": "17a718532450c9c080c8fa8bf8fb5ec276240119",
  "evidenceClass": "temp_workspace_bounded_recall",
  "tempWorkspaceRoot": "<repo>/tmp/memory-recall-temp-workspace-evidence/<run-id>",
  "seedRecordCount": 4,
  "queryCount": 4,
  "expectedResultMatched": true,
  "expectedPrimaryId": "temp-recall-expected-current",
  "irrelevantResultsSuppressed": true,
  "freshnessOrderingAccepted": true,
  "folderScopeAccepted": true,
  "timeoutErrorCode": "SEARCH_MEMORY_TIMEOUT",
  "timeoutJsonRpcCode": -32002,
  "providerCalls": 0,
  "realMemoryReads": 0,
  "jsonlReads": 0,
  "durableMemoryWrites": 0,
  "durableAuditWrites": 0,
  "rawContentOutput": false,
  "cleanupVerified": true,
  "readinessClaimAllowed": false,
  "decision": "TEMP_WORKSPACE_RECALL_EVIDENCE_ACCEPTED_NOT_READY"
}
```

Sanitization boundary:

- Raw seed `content` is not included in the evidence object.
- Real memory IDs are not included.
- Real paths are represented as sanitized temp-root shape.
- `readinessClaimAllowed` remains `false`.

## Side-Effect Boundary

Counters asserted by the targeted test:

| counter | value |
|---|---:|
| `providerCalls` | `0` |
| `realMemoryReads` | `0` |
| `jsonlReads` | `0` |
| `durableMemoryWrites` | `0` |
| `durableAuditWrites` | `0` |
| `queryCount` | `4` |
| `seedRecordCount` | `4` |
| `cleanupVerified` | `true` |

No true live `search_memory` against the real store is executed.

No real memory content is read.

No `.jsonl` audit or durable memory content is read.

No provider is called.

No broad real memory scan is performed.

No durable memory or audit write is performed.

No migration/import/export/backup/restore apply is performed.

No public MCP expansion, config/watchdog/startup change, package/lockfile change, tag, release, deploy, cutover, force push, branch rewrite, or readiness claim occurs.

## Decision

The temp workspace evidence is accepted as bounded evidence that the synthetic temp-workspace recall path can:

- use an isolated temp root,
- seed exactly four synthetic records,
- execute exactly four bounded queries,
- return the expected current synthetic result,
- suppress irrelevant synthetic results,
- cover freshness and folder behavior in the temp workspace,
- return bounded timeout/error shape,
- emit sanitized evidence,
- verify cleanup,
- preserve no-provider, no-real-memory, no-.jsonl-read, and no-durable-write boundaries.

This evidence is still not enough to claim `memory recall reliable` because it does not execute true live `search_memory` against the real store, does not read real memory, does not evaluate a real corpus, and does not prove production recall quality.

`memory recall reliable`: not claimed.

`memory write reliable`: not claimed.

Runtime ready: not claimed.

RC ready: not claimed.

Production ready: not claimed.

V8 implemented: not claimed.

VCP full parity: not claimed.

`RC_NOT_READY_BLOCKED` remains.

Result: `MEMORY_RECALL_TEMP_WORKSPACE_EVIDENCE_COMPLETED_NOT_READY`.
