# Memory Recall Limited Local Real-Path Bounded Evidence Execution

Status: `MEMORY_RECALL_LIMITED_LOCAL_REAL_PATH_BOUNDED_EVIDENCE_COMPLETED_NOT_READY`
Date: 2026-05-22
Scope: limited local real-path bounded recall evidence execution
Baseline: `3f42bb5f59e262e14b48c07cf7e1b0f33c5dadd7`
Controlling state: `RC_NOT_READY_BLOCKED`

## Purpose

This packet executes the CM-0760 planning boundary.

It exercises checked-in local recall-path modules against a run-specific temp root and synthetic records only. It remains a bounded evidence layer between the CM-0758 temp-workspace harness and any future true live real-store `search_memory`.

This packet does not execute true live `search_memory` against the real store, does not execute true live `record_memory`, does not read real memory content, does not read `.jsonl` audit or durable memory content, does not call providers, does not write durable memory or audit state, and does not claim memory recall reliability.

## Execution Surface

Targeted test:

```text
node --test tests\memory-recall-limited-local-real-path-evidence.test.js
```

Targeted result:

```text
pass: 1/1
```

Changed execution file:

```text
tests/memory-recall-limited-local-real-path-evidence.test.js
```

The test is fixture/temp-root/local-only. It does not call MCP `search_memory`, does not use the real runtime store, and does not read real diary, audit, shadow-store, `.jsonl`, or user-owned memory content.

## Local Real-Path Modules Exercised

The targeted test exercises these checked-in local recall-path modules:

- `VectorIndexStore` using local-hash embeddings and a temp-root vector index file.
- `CandidateGenerator` using the temp-root synthetic chunk source and read-only query mode.
- `KnowledgeBaseRecallPipeline` using read-only search mode.
- `RecallEnhancer` for local result ordering.
- `runSearchMemoryWithTimeout()` for bounded timeout/error shape.

Temp-only local artifacts:

- synthetic `.json` seed records under `<temp-root>/records`;
- temp vector index under `<temp-root>/index/vector-index.json`;
- no real store path;
- no `.jsonl` path;
- no provider path;
- no durable memory or audit path.

## Temp Root Evidence

Root policy implemented:

- Parent root: `<repo>/tmp/memory-recall-limited-local-real-path-evidence`.
- Run root: `<repo>/tmp/memory-recall-limited-local-real-path-evidence/CM-0761-<run-id>`.
- The test resolves the parent and run root before use.
- The run root must start with the resolved temp parent path.
- The test creates exactly one run-specific temp root.
- The run root contains only synthetic records and temp local-path artifacts generated by the test.
- The seed files use `.json`, not `.jsonl`.
- The test fails closed if the run root is outside the expected temp parent.

Cleanup evidence:

- Cleanup target is the resolved run-specific temp root only.
- Cleanup occurs after the four bounded queries.
- Cleanup verifies the run root no longer exists.

## Synthetic Seed Evidence

The test writes exactly four synthetic seed records to the temp root:

| memoryId | target | folder | role |
|---|---|---|---|
| `local-realpath-expected-current` | `process` | `alpha` | expected current result |
| `local-realpath-expected-older` | `process` | `alpha` | expected older result |
| `local-realpath-irrelevant-same-folder` | `process` | `alpha` | irrelevant same-folder result |
| `local-realpath-irrelevant-other-folder` | `process` | `beta` | irrelevant other-folder result |

Seed boundaries:

- Seeds are synthetic and non-sensitive.
- Seeds are generated inside the temp root only.
- Seeds are not copied from real memory.
- Seeds are not copied from `.jsonl` audit or durable memory content.
- Seeds exercise expected match, irrelevant suppression, freshness ordering, and folder filtering.

## Query Evidence

Exact query count:

```text
queryCount = 4
```

Executed bounded query set:

| query # | purpose | query / scope | accepted evidence |
|---:|---|---|---|
| 1 | expected result | `local alpha bounded recall` | returns `local-realpath-expected-current` first and `local-realpath-expected-older` second |
| 2 | irrelevant suppression | `local alpha bounded recall` | excludes `local-realpath-irrelevant-same-folder` and `local-realpath-irrelevant-other-folder` from accepted results |
| 3 | folder behavior | `local alpha bounded recall`, folder scope `alpha` | excludes beta-folder record `local-realpath-irrelevant-other-folder` |
| 4 | timeout/error boundary | `local alpha bounded recall timeout`, bounded timeout `5 ms` | returns `SEARCH_MEMORY_TIMEOUT` with JSON-RPC code `-32002` |

No additional query is executed by the test.

## Sanitized Evidence Object

The test constructs a sanitized evidence object equivalent to:

```json
{
  "taskId": "MEMORY_RECALL_LIMITED_LOCAL_REAL_PATH_BOUNDED_EVIDENCE_EXECUTION",
  "baseline": "3f42bb5f59e262e14b48c07cf7e1b0f33c5dadd7",
  "evidenceClass": "limited_local_real_path_bounded_recall",
  "tempRootSanitized": "<repo>/tmp/memory-recall-limited-local-real-path-evidence/CM-0761-<run-id>",
  "seedCount": 4,
  "queryCount": 4,
  "expectedResultIds": [
    "local-realpath-expected-current",
    "local-realpath-expected-older"
  ],
  "suppressedResultIds": [
    "local-realpath-irrelevant-same-folder",
    "local-realpath-irrelevant-other-folder"
  ],
  "folderScope": {
    "folder": "alpha",
    "betaSuppressed": true
  },
  "freshnessOrder": {
    "first": "local-realpath-expected-current",
    "second": "local-realpath-expected-older"
  },
  "timeoutError": {
    "code": "SEARCH_MEMORY_TIMEOUT",
    "jsonRpcCode": -32002
  },
  "sideEffectCounters": {
    "providerCalls": 0,
    "realMemoryReads": 0,
    "jsonlReads": 0,
    "durableMemoryWrites": 0,
    "durableAuditWrites": 0
  },
  "rawContentOutput": false,
  "cleanupVerified": true,
  "readinessClaimAllowed": false,
  "decision": "LIMITED_LOCAL_REAL_PATH_BOUNDED_EVIDENCE_ACCEPTED_NOT_READY"
}
```

Sanitization boundary:

- Raw synthetic `content` is not included in the evidence object.
- Real memory IDs are not included.
- Real paths are represented as sanitized temp-root shape.
- `readinessClaimAllowed` remains `false`.

## Side-Effect Boundary

Counters asserted by the targeted test:

| counter | value |
|---|---:|
| `providerCalls` | `0` |
| `realMemoryReads` | `0` |
| `jsonlReads` | `0` |
| `durableMemoryWrites` | `0` |
| `durableAuditWrites` | `0` |
| `queryCount` | `4` |
| `seedCount` | `4` |
| `cleanupVerified` | `true` |

No true live `search_memory` against the real store is executed.

No true live `record_memory` is executed.

No real memory content is read.

No `.jsonl` audit or durable memory content is read.

No provider is called.

No broad real memory scan is performed.

No durable memory or audit write is performed.

No migration/import/export/backup/restore apply is performed.

No public MCP expansion, config/watchdog/startup change, package/lockfile change, tag, release, deploy, cutover, force push, branch rewrite, or readiness claim occurs.

## Decision

The limited local real-path evidence is accepted as bounded evidence that a synthetic temp-root local recall-path packet can:

- use an isolated temp root,
- seed exactly four synthetic records,
- create temp-root local vector-index artifacts,
- execute exactly four bounded local recall-path queries,
- return the expected current synthetic result,
- suppress irrelevant synthetic results,
- cover freshness ordering and alpha folder behavior,
- return bounded timeout/error shape,
- emit sanitized evidence,
- verify cleanup,
- preserve no-provider, no-real-memory, no-.jsonl-read, and no-durable-memory/audit boundaries.

This evidence is still not enough to claim `memory recall reliable` because it does not execute true live `search_memory` against the real store, does not read real memory, does not evaluate a real corpus, does not prove provider-backed quality, and does not prove production recall behavior.

`memory recall reliable`: not claimed.

`memory write reliable`: not claimed.

Runtime ready: not claimed.

RC ready: not claimed.

Production ready: not claimed.

V8 implemented: not claimed.

VCP full parity: not claimed.

`RC_NOT_READY_BLOCKED` remains.

Result: `MEMORY_RECALL_LIMITED_LOCAL_REAL_PATH_BOUNDED_EVIDENCE_COMPLETED_NOT_READY`.
