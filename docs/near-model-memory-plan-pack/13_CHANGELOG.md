# 13｜Changelog

## v1.0

生成完整计划包。

### 核心内容

- 调整目标表达。
- 合并 Codex 回复中的底座能力路线。
- 增加 memory context layer。
- 增加 `prepare_memory_context`。
- 增加 `propose_memory_delta`。
- 增加 operator-only full surface 阶段。
- 增加 native write production proof 阶段。
- 增加两轮自审报告。
- 增加外部审查申请书。
- 增加 Codex 执行任务书。

### 关键判断

```text
能实现接近模型内置记忆体验。
但不是靠默认全开 MCP tools。
而是靠任务前自动 memory context package。
```
