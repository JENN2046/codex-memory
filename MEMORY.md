# codex-memory Memory

最后更新：2026-05-05

## 当前边界

- `A:\VCP\VCPToolBox` 与 `A:\VCP\VCPChat` 只作为 donor / behavior reference。
- 后续实现继续只落在 `A:\codex-memory`。
- 不再把 donor 项目当作运行时依赖，也不继续在 donor 仓库里做功能开发。
- `docs/` 目录和治理/路线类文档现已纳入版本控制，作为当前仓库的参考与治理资料存在，但不覆盖 README / source / tests 所代表的运行时现实。

## 2026-05-05 项目记忆

- 当前 `main` 已推送到 `origin/main`，最新提交是 `dd2fb50 docs: add phase e mainline gate checkpoint 02`。
- 上一个文档收口提交是 `28a8fa9 chore(docs): add phase e http observability 03 snapshot and links`；当前远端主分支已连续包含这两次文档收口。
- 新增运行检查点 [phase-e-mainline-gate-checkpoint-02.md](/A:/codex-memory/logs/phase-e-mainline-gate-checkpoint-02.md)，并已从 [PHASE_NAVIGATION.md](/A:/codex-memory/PHASE_NAVIGATION.md) / [PHASE_E_DAILY_SELF_CHECK.md](/A:/codex-memory/PHASE_E_DAILY_SELF_CHECK.md) 挂到“日常运维 / 排障 / 回滚”入口。
- 文档收口后的 [phase-e-mainline-gate-checkpoint-02.md](/A:/codex-memory/logs/phase-e-mainline-gate-checkpoint-02.md) 与 `npm run gate:mainline` 已通过：
  - `status: ok`
  - health `200` at `http://127.0.0.1:7605/health`
  - compare `34/34 matched`
  - rollback `34/34 rollback-ready`
- 当前可把这组结果当成新的 Phase E 维护锚点：它证明文档基线调整没有破坏默认 HTTP MCP 主链、compare 标准 suite 或 rollback readiness。
- 当前仓库状态在这次检查点完成后为：`main` 本地与 `origin/main` 对齐，工作区 clean。

## 2026-04-26 项目记忆

- 当前 `main` 已推送到 `origin/main`，最新提交是 `618d854 docs: add vcp memory capability assessment`。
- 最新 CI 已通过：`https://github.com/JENN2046/codex-memory/actions/runs/24959264708`。
- `cleanup-legacy-chunks --confirm` 已真实执行过，删除 `39` 个 legacy chunks；执行前已备份 SQLite 到 `data/backups/codex-memory-before-legacy-cleanup-20260426-222138.sqlite`。
- 当前 `profile-health` 状态为 `ready`，当前 fingerprint 是 `bge-m3-local__1024__v1`，`legacy chunks` 已归零。
- 清理后 `profile-gate --json --summary-only` 可运行，但当前返回 `warn / baseline-missing`；这是因为没有可比 baseline profile，不代表当前 profile 失败。
- `EMBEDDING_PROFILE_MIGRATION_RUNBOOK.md` 已补充 legacy cleanup 流程，明确清理前要做 dry-run、备份，以及清理后 `baseline-missing` 的解释。
- GitHub Actions 的 Node.js 20 action deprecation 已处理：`.github/workflows/ci.yml` 已升级到 `actions/checkout@v6` 和 `actions/setup-node@v6`，CI 通过且不再显示该 annotation。
- 新增 `VCP_MEMORY_CAPABILITY_ASSESSMENT.md`，把当前浪潮对 VCP 记忆主链的能力评估写成正式项目文档：
  - VCP 记忆主链约 `7.5 - 8 / 10`
  - 完整 VCP 生态约 `4 - 5 / 10`
  - 下一步重点是真实 previous profile baseline、donor 手感继续收口、V8 真实 query 效果评估
- 当前最值得接的下一步不是继续清理数据，而是补真实 baseline 门禁：在下次 embedding profile 切换前保留 previous fingerprint，并使用 `profile-gate --baseline-fingerprint "<old-profile>" --require-pass` 做硬门槛。

## 今天的关键决策

- `Phase C` 不再停留在“空索引懒回填”，而是升级为：
  - `force rebuild`
  - `incremental sync`
  - 查询入口自动追平 donor 变化
- donor root 一旦通过显式 rebuild 导入，就会被当前运行态记住，后续 `search / listTopics / getTopicContent` 可以继续沿用。
- 增量同步默认带 cooldown，配置项是 `CODEX_MEMORY_ACTIVE_MEMORY_SYNC_MIN_INTERVAL_MS`，默认值 `5000` 毫秒。
- 测试或调试时允许把这个值设成 `0`，避免 cooldown 干扰回归。
- `Phase C` 的主动记忆兼容继续只落在 `A:\codex-memory`，不回头修改 donor 项目。
- `Phase D` 第一刀优先落“外层兼容包装”，而不是回头改 Phase C 核心：
  - donor 风格 `DeepMemo` CLI
  - donor 风格 `TopicMemo` CLI
  - 只读 compare harness
- Phase D 的第二刀继续落“观测与回归”：
  - active-memory `health / rebuild / sync` CLI
  - compare harness 的 success-path donor 对照
- Phase D 的第三刀继续落“回滚判断”：
  - compare harness 增加 `coreDiff / extendedDiff`
  - 只读 rollback readiness CLI
- Phase D 的第四刀继续落“批量样本”：
  - compare harness 增加 `suite` 模式
  - compare 输出 `aggregateDiff`
  - rollback readiness 增加 `suite` 模式
- Phase D 的第五刀已经把“仓库标准集”沉淀下来了：
  - `benchmarks/active-memory-suite/standard-suite.json`
  - `benchmarks/active-memory-suite/vchat-fixture/`
  - `benchmarks/active-memory-suite/legacy/standard-legacy-runner.js`
  - compare / rollback 不再只依赖临时测试 case
- Phase D 的第六刀继续把标准集从“能跑”扩成“有覆盖”：
  - 标准集先扩到 `11` 个 mixed-tool case
  - `DeepMemo` 增加高级查询语法成功 case
  - `TopicMemo` 增加空历史 / 缺历史 / 缺话题 case
- Phase D 的第七刀把 donor 配置边界也收进标准集：
  - compare suite 支持按-case `env`
  - `DeepMemo` 增加 blocked-keyword 过滤成功 case
  - `DeepMemo` 增加 all-keywords-blocked 错误 case
  - `DeepMemo` 增加 rerank-failure fallback 成功 case
- Phase D 的第八刀继续把“标准集能覆盖的 donor 边界”再往前推：
  - 标准集扩到 `15` 个 mixed-tool case
  - `DeepMemo` 增加多 topic 排序 case
  - `DeepMemo` 增加 `agent-not-found` case
  - `TopicMemo` 增加 `agent-not-found` case
  - `TopicMemo` 增加 `settings.userName` 缺失时回退到 `主人` 的 case
  - compare suite 的按-case `env` 现在会自动解析 `_PATH / _ROOT / _DIR` 相对路径到 suite 目录
- Phase D 的第九刀继续把标准集从“单 maid”扩到“多 agent / 多 fixture / 更复杂排序”：
  - 标准集扩到 `19` 个 mixed-tool case
  - 新增 `vchat-fixture-multi-agent`
  - 新增 `vchat-fixture-ranking`
  - `DeepMemo` 增加多 agent / 多 fixture 成功检索 case
  - `DeepMemo` 增加单 topic 多窗口复杂排序 case
  - `TopicMemo` 增加多 agent `ListTopics / GetTopicContent` case
  - 仓库标准 runner 现在能按 `maid + root` 动态解析 fixture agent
- Phase D 的第十刀继续把 donor 组合语义收进仓库门禁：
  - 标准集扩到 `22` 个 mixed-tool case
  - 新增 `vchat-fixture-ranking-extended`
  - `DeepMemo` 增加多 agent 默认 `exclude_latest` case
  - `DeepMemo` 增加多 agent `current_topic_id` 显式排除 case
  - `DeepMemo` 增加单 topic 三窗口排序稳定性 case
- Phase D 的第十一刀继续把“手工可跑性”和“别名/混合过滤/更大排序集”一起收口：
  - 标准集扩到 `25` 个 mixed-tool case
  - 新增 `vchat-fixture-multi-topic-large`
  - `DeepMemo` 增加 maid alias 命中 case
  - `DeepMemo` 增加更大的跨多 topic 排序 case
  - `TopicMemo` 增加 `agentId + maid` 混合过滤 case
  - 默认主 fixture 的 case 现在也显式带 `CODEX_MEMORY_ACTIVE_MEMORY_ROOT=./vchat-fixture`
- Phase D 的第十二刀开始收“跨环境稳定性”：
  - compare suite 现在支持 fixture manifest 预处理
  - fixture root 如果带 `.codex-fixture-manifest.json`，suite 会复制到临时目录再打固定 `mtime`
  - 当前 `6` 个标准 fixture root 已全部落 manifest
  - `deepmemo-large-multi-topic-order-success` 的标准基线现收敛为稳定顺序：`gamma -> beta -> alpha`
- Phase D 的第十三刀补了 manifest 覆盖守门：
  - 回归现在会扫描所有 `vchat-fixture*` 根目录
  - 标准 suite 里引用的 fixture root 也必须遵守 `vchat-fixture*` 命名，并且位于 `benchmarks/active-memory-suite` 下
  - 每个 fixture 根目录都必须带 manifest
  - 每个 fixture 里的每个 `history.json` 都必须出现在 manifest 里
- Phase D 的第十四刀补了标准 suite case 分类元数据：
  - `standard-suite.json` 根级新增 `metaVersion / metaSchema`
  - 每个 case 新增 `meta.category / meta.expectation / meta.fixture / meta.tags`
  - compare suite 报告现在会透传 `metaVersion / metaSchema / cases[*].meta`
  - 回归会校验分类元数据完整性和 fixture 一致性
- Phase D 的第十五刀开始消费这层分类元数据：
  - compare suite 报告新增 `categoryAggregate`
  - rollback suite 报告也新增 `categoryAggregate`
  - 当前会按 `meta.category` 汇总 case 数、匹配数、ready 数、mismatch 数和 fixture 分布
- Phase D 的第十六刀补了 suite 分类消费能力：
  - compare / rollback suite CLI 新增 `--category <meta.category>` 过滤
  - `categoryAggregate` 新增按 `fixture` 的二级分组 `fixtureAggregate`
  - 标准 suite 回归现在会校验 category 过滤和 fixture 二级聚合
- Phase D 的第十七刀补了 suite 的 fixture/tag 过滤：
  - compare / rollback suite CLI 新增 `--fixture <meta.fixture>` 过滤
  - compare / rollback suite CLI 新增 `--tag <meta.tags item>` 过滤
  - 两者支持逗号分隔 OR 匹配
  - 标准 suite 回归现在会校验 fixture/tag 过滤输出
- Phase D 的第十八刀补了 suite 的 expectation/tool 过滤：
  - compare / rollback suite CLI 新增 `--expectation <meta.expectation>` 过滤
  - suite 模式下 `--tool` 现在也真正作为过滤条件生效
  - 标准 suite 回归现在会校验 expectation/tool 过滤输出
- Phase D 的第十九刀补了 suite 的组合过滤能力：
  - compare / rollback suite CLI 新增 `--tag-all <meta.tags>`（AND）
  - 新增 `--exclude-tag <meta.tags>`（排除）
  - 新增 `--exclude-fixture <meta.fixture>`（排除）
  - suite 透传 `tagAllFilter / excludeTagFilter / excludeFixtureFilter`
- Phase D 的第二十刀补了 suite 分类过滤可组合：
  - compare / rollback suite CLI 的 `--category` 支持逗号分隔 OR（例如 `ordering,agent-selection`）
  - 同步校验 `categoryFilter` 多值序列化与 `caseName` 选择结果
- Phase D 的第二十一刀把 `--expectation` 多值语义再固化了一层：
  - README 和 suite README 已明确 `--expectation <meta.expectation>[,<meta.expectation>...]`
  - compare / rollback 回归已覆盖 `error,success`
  - compare / rollback 回归已覆盖 `success,error`，确认过滤集合会做顺序归一化
  - compare / rollback 回归已覆盖 `success,error,success`，确认过滤集合会自动去重
  - 可读性断言同时锁定了 case 总量、fixture 准备数量和 `meta.expectation` 值域
- Phase D 的第二十二刀把 rollback readiness 的“为什么不 ready”结构化了：
  - suite summary 新增 `recommendationBreakdown / blockerBreakdown`
  - `categoryAggregate` 新增 `recommendationBreakdown / blockerBreakdown`
  - `fixtureAggregate` 新增 `recommendationBreakdown / blockerBreakdown`
  - README 已同步说明这组字段
  - 回归新增 mixed-reason suite，覆盖 `rollback-safe + investigate-before-rollback + legacy-unavailable`
- Phase D 的第二十三刀把 compare harness 也补成了对称的“差异原因视角”：
  - suite summary 新增 `comparisonBreakdown / driftReasonBreakdown`
  - `categoryAggregate` 新增 `comparisonBreakdown / driftReasonBreakdown`
  - `fixtureAggregate` 新增 `comparisonBreakdown / driftReasonBreakdown`
  - `comparisonBreakdown` 聚合 `matched / mismatched / legacy-unavailable`
  - `driftReasonBreakdown` 聚合 `status-mismatch / exit-code-mismatch / result-mismatch / error-mismatch / core-diff / extended-only-drift`
  - README 已同步 compare suite 输出说明
  - 回归新增 mixed-reason compare suite，覆盖 `matched + mismatched + legacy-unavailable`
- Phase D 的第二十四刀把 compare 的 case 级 reason 也补齐了：
  - `cases[*].comparison.outcome`
  - `cases[*].comparison.driftReasons`
  - suite 文本输出里的 case 预览现在会直接打印 `outcome` 和 `driftReasons`
  - compare 回归已锁定 matched / mismatched / legacy-unavailable 三类 case 的 case-level reason 语义
- Phase D 的第二十五刀把 rollback 的 case 级 reason 也补齐了：
  - `cases[*].summary.outcome`
  - `cases[*].summary.blockerReasons`
  - ready case 会稳定给出 `outcome=rollback-safe` 和空 `blockerReasons`
  - mismatch case 会稳定给出更细原因，例如 `core-diff / result-mismatch`
  - legacy 缺失 case 会稳定给出 `legacy-unavailable`
  - rollback 文本输出里的 case 预览现在会直接打印 `outcome` 和 `blockerReasons`
- Phase D 的第二十六刀开始收文本输出的人类可读性：
  - compare suite 文本输出顶部新增紧凑 `comparison-breakdown / drift-reason-breakdown`
  - rollback suite 文本输出顶部新增紧凑 `recommendationBreakdown / blockerBreakdown`
  - compare / rollback 的 `categoryAggregate` / `fixtureAggregate` 行内现在会直接预览 reason
  - compare / rollback 各补了一条文本模式回归，锁定关键摘要行与 case 预览行
- Phase D 的第二十七刀开始收 README 的真实操作手册：
  - compare 段落新增“先看文本摘要 -> 再按 category/fixture 缩范围 -> 最后切 JSON 深挖”的推荐流程
  - rollback 段落新增“先看 blocker 摘要 -> 再缩范围 -> 最后接 require-ready 门禁”的推荐流程
  - README 不再只列参数，也开始承载实际排障路径
- Phase D 的第二十八刀开始收“独立迁移验收清单”：
  - 新增 [PHASE_D_MIGRATION_ACCEPTANCE_CHECKLIST.md](/A:/codex-memory/PHASE_D_MIGRATION_ACCEPTANCE_CHECKLIST.md)
  - 把 compare / rollback 现有能力整理成一份切换前可直接照着执行的 checklist
  - 清单覆盖文本模式、JSON 模式、`--require-match`、`--require-ready`、suite 稳定性、阻断项与切换建议
  - 后续正式切主链前，优先以这份清单做一次完整验收，而不是只靠零散 README 命令
- Phase D 的第二十九刀开始跑“首轮独立迁移验收”：
  - `npm test` 跑出 `80/80`
  - `node --test compare + rollback` 跑出 `25/25`
  - `compare --suite standard-suite --json --require-match` 通过，`matchedCaseCount=25`、`mismatchedCaseCount=0`
  - `rollback --suite standard-suite --json --require-ready` 通过，`readyCaseCount=25`、`notReadyCaseCount=0`
  - 当前结论不是纯 `Pass`，而是 `Pass with Known Gaps`
  - 已知差距集中在 compare 的 `extended-only-drift=25`、`extendedMismatchCountTotal=185`
  - 当前没有 blocker，可进入“灰度切主链 + compare/rollback 持续守门”阶段
- Phase D 的第三十刀开始收“灰度切主链 playbook”：
  - 新增 [PHASE_D_GRAY_ROLLOUT_PLAYBOOK.md](/A:/codex-memory/PHASE_D_GRAY_ROLLOUT_PLAYBOOK.md)
  - 把灰度执行顺序、观察项、暂停条件、回滚动作写成可直接执行的操作手册
  - 当前建议已经从“先做验收”进入“按 playbook 做灰度切主链观察”
- Phase D 的第三十一刀开始收“灰度执行记录模板”：
  - 新增 [PHASE_D_GRAY_ROLLOUT_LOG_TEMPLATE.md](/A:/codex-memory/PHASE_D_GRAY_ROLLOUT_LOG_TEMPLATE.md)
  - 把每轮灰度的门禁结果、MCP 观察、继续/暂停/回滚判断收成统一模板
  - 后续真实灰度时，不再零散写结论，直接按模板逐轮落盘
- Phase D 的第三十二刀开始落第一份真实灰度记录：
  - 新增 [phase-d-gray-rollout-gray-01.md](/A:/codex-memory/logs/phase-d-gray-rollout-gray-01.md)
  - 当前 `Gray-01` 不是“已切主链观察”，而是“灰度前基线记录”
  - 记录了当前门禁结果：`npm test=80/80`、compare `25/25 matched`、rollback `25/25 rollback-safe`
  - 当前判断是继续灰度，下一步进入 `Gray-02` 的真实主入口切换观察
- Phase D 的第三十三刀开始收 `Gray-02` 执行前清单：
  - 新增 [PHASE_D_GRAY_02_PRECHECK.md](/A:/codex-memory/PHASE_D_GRAY_02_PRECHECK.md)
  - 已明确真实切换点主要在 Codex 接入层：`vcp_codex_memory` 的 HTTP MCP 主入口
  - 已明确默认目标地址：`http://127.0.0.1:7605/mcp/codex-memory`
  - 已明确切换前命令、回退点和现场证据
- Phase D 的第三十四刀开始落 `Gray-02` 预检查通过记录：
  - 新增 [phase-d-gray-rollout-gray-02-precheck-pass.md](/A:/codex-memory/logs/phase-d-gray-rollout-gray-02-precheck-pass.md)
  - 已实跑：`npm test`、`start:http:ensure`、HTTP `/health`、compare `--require-match`、rollback `--require-ready`
  - 当前配置文件 `C:\Users\617\.codex\config.toml` 已指向 `http://127.0.0.1:7605/mcp/codex-memory`
  - 当前 `Gray-02` 不再缺“配置切换”，只差一次新会话 / 新线程观察来完成真实主入口灰度验证
- Phase D 的第三十五刀开始落 `Gray-02` 真实观察记录：
  - 新增 [phase-d-gray-rollout-gray-02.md](/A:/codex-memory/logs/phase-d-gray-rollout-gray-02.md)
  - 用户重启后已成功回到当前会话，当前未出现 `vcp_codex_memory` 初始化失败或握手超时
  - 重启后再次实跑 HTTP `/health`、compare、rollback，结果仍保持全绿
  - 当前可把 `Gray-02` 判定为第一次真实主入口灰度观察通过
  - 下一步进入 `Gray-03`，继续观察真实使用下的持续稳定性
- Phase D 的第三十六刀开始落 `Gray-03` 稳定性观察记录：
  - 新增 [phase-d-gray-rollout-gray-03.md](/A:/codex-memory/logs/phase-d-gray-rollout-gray-03.md)
  - 已实跑：HTTP `/health`、`mcp-contract.test.js`、`mcp-http.test.js`、compare、rollback
  - 当前运行态已不只是“切上去能用”，而是“持续稳定”
  - 当前可开始讨论从“灰度主链”推进到“默认主链”
- Phase D 的第三十七刀开始收 `Gray-03` 的持续稳定性观察计划：
  - 新增 [PHASE_D_GRAY_03_STABILITY_PLAN.md](/A:/codex-memory/PHASE_D_GRAY_03_STABILITY_PLAN.md)
  - 固定 `Gray-04 / Gray-05` 的观察目标、必查项、继续/暂停标准
  - 新增 [PHASE_D_GRAY_FOLLOWUP_LOG_SKELETON.md](/A:/codex-memory/PHASE_D_GRAY_FOLLOWUP_LOG_SKELETON.md)
  - 后续 1-2 轮真实使用观察可以直接按骨架落盘
- Phase D 的第三十八刀开始落 `Gray-04` 稳定性观察记录：
  - 新增 [phase-d-gray-rollout-gray-04.md](/A:/codex-memory/logs/phase-d-gray-rollout-gray-04.md)
  - 当前继续按 `Gray-03` 计划执行，没有新增代码变更或入口改动
  - compare 继续 `25/25 matched`
  - rollback 继续 `25/25 rollback-safe`
  - HTTP 日志尾部未出现新的异常迹象
  - 下一步进入 `Gray-05`
- Phase D 的第三十九刀开始落 `Gray-05` 稳定性观察记录：
  - 新增 [phase-d-gray-rollout-gray-05.md](/A:/codex-memory/logs/phase-d-gray-rollout-gray-05.md)
  - `Gray-03` 计划中的后续两轮稳定性观察现已全部完成
  - compare 继续 `25/25 matched`
  - rollback 继续 `25/25 rollback-safe`
  - 当前已具备整理“默认主链切换结论”的条件
- Phase D 的第四十刀开始落“默认主链切换结论与收官说明”：
  - 新增 [PHASE_D_DEFAULT_MAINLINE_CONCLUSION.md](/A:/codex-memory/PHASE_D_DEFAULT_MAINLINE_CONCLUSION.md)
  - 当前正式结论：`codex-memory` 可以作为 `vcp_codex_memory` 的默认主链实现
  - donor / 旧实现回到只读对照与回滚参考角色
  - 当前剩余工作不再是“能不能独立出来”，而是 donor 扩展 payload 的精修
- Phase E 的第一刀开始整理“后续精修 backlog”：
  - 新增 [PHASE_E_BACKLOG.md](/A:/codex-memory/PHASE_E_BACKLOG.md)
  - 已按 `P0 / P1 / P2` 和“主线 / 精修线 / 可维护性”分组
  - 当前建议的起步顺序是：
    - 先把 compare / rollback 持续门禁、运行态可观测性、回滚流程再收紧
    - 再继续收扩展字段 drift 与标准集覆盖
- Phase E 的第二刀开始把“持续门禁常态化”落成仓库标准入口：
  - 新增 `src/cli/mainline-gate.js`
  - 新增 `npm run gate:mainline`
  - 新增 `npm run gate:mainline:strict`
  - 默认日常 gate 检查：HTTP `/health`、compare `--require-match`、rollback `--require-ready`
  - 严格 gate 额外检查：`mcp-contract.test.js + mcp-http.test.js + npm test`
  - compare / rollback 改为直连底层 CLI，而不是经 `npm run` 包裹，避免 npm 前导输出污染 JSON 解析
  - gate CLI 定向回归已新增并通过，真实 `gate:mainline` 与 `gate:mainline:strict` 也已跑通
- Phase E 的第三刀开始把“HTTP MCP 运行态可观测性”落成仓库标准入口：
  - 新增 `src/cli/http-observe.js`
  - 新增 `npm run observe:http`
  - 汇总 `/health`、HTTP 日志、watchdog 日志、bridge audit、recall audit
  - 输出 `ok / warn / error` 三级运行态诊断结论
  - 当前真实环境里 `observe:http` 返回 `warn`，原因是 watchdog 日志里存在历史恢复记录，但 `/health`、bridge audit、recall audit 都正常
  - 这条入口的目标是把“服务活着但最近掉过 / 恢复过 / 没有最近流量”这种问题一次性看清，而不是只看 `/health`
- Phase E 的第四刀开始把“默认主链回滚流程”落成仓库标准入口：
  - 新增 `src/cli/mainline-rollback.js`
  - 新增 `npm run rollback:mainline:plan`
  - 读取当前 `C:\Users\617\.codex\config.toml` 中的 `[mcp_servers.vcp_codex_memory]`
  - 可自动从 `A:\VCP\VCPToolBox\config.env` 推断 legacy MCP URL
  - 当前自动推断出的 legacy target 是 `http://127.0.0.1:6005/mcp/codex-memory`
  - 输出当前入口模式、legacy target、最小 rollback patch 和回滚步骤
  - 当前真实环境里这条 CLI 已返回 `ok`，旧服务已在 `127.0.0.1:6005` 上可达
  - 这条入口的目标是把“当前到底连到哪”“如果要切回去最小改哪几行”收成一份标准报告，而不是继续靠 Gray 文档手抄
- Phase E 的第五刀把 donor 回滚链做成一次真实可逆演练：
  - 先备份 `C:\Users\617\.codex\config.toml`
  - 临时把 `[mcp_servers.vcp_codex_memory]` 从 `7605` 切到 `6005`
  - donor `initialize` 返回 `200 + Mcp-Session-Id`
  - donor `tools/list` 返回 `record_memory / search_memory / memory_overview`
  - 随后立刻把配置切回 `7605`
  - 切回后 `npm run gate:mainline` 继续通过
  - 这意味着当前回滚链已经从“计划可生成、目标可达”推进到“配置切换闭环已实际走通”
- Phase E 的第六刀开始收 `P1-1` 扩展字段 drift：
  - 不改 adapter/service 的 donor 行为，只改 CLI 输出整形
  - `DeepMemo / TopicMemo` 在 `--full` 和错误输出下，把 donor 不需要的顶层扩展字段下沉到 `meta`
  - 这样保留了调试信息，但 compare/rollback 的顶层契约更贴 donor
  - 标准 active-memory suite 现已从 `extended-only-drift=25`、`extendedMismatchCountTotal=185` 收到 `0/0`
  - 当时 `compare --require-match` 是 `matched=25` 且 `driftReasonBreakdown={}`
  - 当时 `rollback --require-ready` 是 `ready=25` 且 `extendedMismatchCountTotal=0`
- Phase E 的第七刀开始收 `P1-2` donor 排序手感：
  - 先不改跨 topic 主排序，只收同分 tie-breaker
  - `ChatHistoryIndexStore.search()` 在同一话题内若 `matchedClauseCount / keywordMatchCount / score` 全相同，改为优先更靠后的 `messageIndex`
  - 新增 `equal-score` fixture 回归，锁住 `hitMessageIndex = [4, 2, 0]`
  - 这条改动只影响同分边界，不影响现有 compare/rollback 标准集
- Phase E 的第八刀继续收 `P1-2` donor 排序手感：
  - 这次开始收跨 topic 的同分边界，但仍然不改 query 语义和召回面
  - `ChatHistoryIndexStore.search()` 在汇总结果后新增最终全局排序：先比 `matchedClauseCount / keywordMatchCount / score`，完全相同再比 `updatedAt`、`hitMessageIndex`
  - 新增 `cross-topic-equal-score` fixture 回归，锁住 `topic_beta_equal` 优先于 `topic_alpha_equal`
  - 这条改动把“更 fresher 的 topic 优先”显式化，不再依赖隐式 conversation 遍历顺序
- Phase E 的第九刀继续收 `P1-2` donor 排序手感：
  - 这次补的是“更紧凑窗口优先”，但明确只作用在跨 topic 的同分结果
  - `ChatHistoryIndexStore.search()` 先区分 same-topic 与 cross-topic：同一 topic 内仍然优先更靠后的 `hitMessageIndex`；不同 topic 才在同分时比较窗口跨度
  - 新增 `compact-window` fixture 回归，锁住 `topic_compact_window` 优先于 `topic_wide_window`
  - 这次中途暴露过一次边界冲突：如果把 compact-window 规则放进同一 topic 排序，会压掉“更新命中窗口优先”；最终实现已把规则收窄，只保留跨 topic 生效
- Phase E 的第十刀继续收 `P1-2` donor 排序手感：
  - 这次不再改实现，而是把“compactness 与 freshness 冲突时谁优先”显式固化
  - 新增 `compact-vs-fresh` fixture 回归，锁住 `topic_compact_older` 仍然排在 `topic_fresh_wider` 前面
  - 这条回归说明当前跨 topic 同分优先级是：先 compactness，再 freshness
  - 这样后续如果要改 donor 手感优先级，会先在回归层看到，而不是默默漂移
- Phase E 的第十一刀继续收 `P1-2` donor 排序手感：
  - 这次补的是最末级 fallback 稳定性，不改实现，只把最后一级 `topicId` 词典序兜底显式锁住
  - 新增 `topicid-fallback` fixture 回归，构造“score / compactness / freshness` 全部打平”的跨 topic case
  - 当前结果固定为 `topic_alpha_fallback` 在 `topic_beta_fallback` 前
  - 到这里为止，P1-2 的排序链已经从主分数到最终兜底都有独立回归守门
- Phase E 的第十二刀转入 `P1` 的错误语义 / 诊断输出精修：
  - 不改 donor 风格错误顶层，继续保持 `status/error/result`
  - 在 CLI 层新增错误 `meta` 归并，支持把输入来源、命令、关键词、topicId 等诊断上下文稳定带出
  - `DeepMemo` 现在已覆盖 `invalid-json` 与 `all-keywords-blocked` 的 `meta` 诊断，并继续补齐 `agent-not-found` 的 donor 别名字段与 `query/rawQuery` 别名
  - `TopicMemo` 现在已覆盖 `agent-not-found` 的 `command / maid / topicId` 诊断
  - `TopicMemo` 的 `topic_id/topicId` 现在也会在无显式 command 时自动推断成 `GetTopicContent`
  - `TopicMemo` 的 `empty-history / history-read-error` 边界也已补齐，前者在 `--full` 下可见，后者继续在 error meta 里带 `historyStatus`
  - 这条线优先收“更好排障但不引入 drift”的信息，因此诊断字段都放在 `meta`
- `TopicMemo` 需要显式区分：
  - `agent-not-found`
  - `topic-not-found`
  - `empty-history`
  - `missing-history`
  - `history-read-error`
- `DeepMemo` 需要保留 donor 风格的成功/失败语义：
  - blocked keyword 会真正影响检索
  - 如果全部关键词都被屏蔽，要拒绝
  - rerank 失败不能打断主流程，要成功回退到原始结果
- `TopicMemo` 的最后几处 donor 展示手感也值得补齐：
  - 锁定话题显示 `🔒`
  - 话题详情里的说话人加粗
  - `settings.userName` 缺失时回退到 `主人`

## 当前实现锚点

- 主动记忆适配层：
  - [index.js](/A:/codex-memory/src/adapters/vcp-active-memory/index.js)
- 主动记忆核心 store：
  - [ChatHistoryIndexStore.js](/A:/codex-memory/src/storage/ChatHistoryIndexStore.js)
- 主动记忆服务：
  - [ActiveRecallService.js](/A:/codex-memory/src/core/ActiveRecallService.js)
- 应用层入口：
  - [app.js](/A:/codex-memory/src/app.js)
- Phase D CLI 入口：
  - [active-memory.js](/A:/codex-memory/src/cli/active-memory.js)
  - [deepmemo.js](/A:/codex-memory/src/cli/deepmemo.js)
  - [topicmemo.js](/A:/codex-memory/src/cli/topicmemo.js)
  - [compare-vcp-active-memory.js](/A:/codex-memory/src/cli/compare-vcp-active-memory.js)
  - [rollback-active-memory.js](/A:/codex-memory/src/cli/rollback-active-memory.js)
  - [mainline-gate.js](/A:/codex-memory/src/cli/mainline-gate.js)
  - [http-observe.js](/A:/codex-memory/src/cli/http-observe.js)
  - [mainline-rollback.js](/A:/codex-memory/src/cli/mainline-rollback.js)
- 配置入口：
  - [createConfig.js](/A:/codex-memory/src/config/createConfig.js)
- 标准 suite 数据集：
  - [standard-suite.json](/A:/codex-memory/benchmarks/active-memory-suite/standard-suite.json)
  - [README.md](/A:/codex-memory/benchmarks/active-memory-suite/README.md)
  - [standard-legacy-runner.js](/A:/codex-memory/benchmarks/active-memory-suite/legacy/standard-legacy-runner.js)
- 主动记忆回归：
  - [phase-c-active-recall.test.js](/A:/codex-memory/tests/phase-c-active-recall.test.js)
- CLI 回归：
  - [active-memory-cli.test.js](/A:/codex-memory/tests/active-memory-cli.test.js)
  - [vcp-active-memory-cli.test.js](/A:/codex-memory/tests/vcp-active-memory-cli.test.js)
  - [compare-vcp-active-memory-cli.test.js](/A:/codex-memory/tests/compare-vcp-active-memory-cli.test.js)
  - [rollback-active-memory-cli.test.js](/A:/codex-memory/tests/rollback-active-memory-cli.test.js)

## 当前有效事实

- `Phase C` 已支持 donor-style VChat 的：
  - 全量 rebuild
  - 空索引自动 backfill
  - 已建索引后的自动增量 sync
  - 新增 / 修改 / 删除 topic 的局部收敛
- `Phase C` 现在还支持 donor 风格的：
  - `DeepMemo/TopicMemo` 中文输出和错误文案
  - `status/result` 顶层包络
  - `DeepMemo` 高级查询语法
  - blocked keyword 过滤
  - rerank failure fallback
  - agent / topic / history 状态分辨
  - `TopicMemo` 锁定图标与加粗说话人展示
  - 默认用户名回退到 `主人`
- `Phase D` 现在还支持：
  - donor 风格 `stdin JSON -> stdout/stderr JSON -> exit code`
  - `DeepMemo/TopicMemo` 独立 CLI 入口
  - `--full` 完整 payload 模式
  - compare harness 对新 CLI 和 legacy script 的只读对照报告
  - active-memory 的 `health / rebuild / sync` 独立 CLI
  - rollback readiness 的只读判断入口
  - compare harness 的 `coreDiff / extendedDiff`
  - compare / rollback 的 `suite` 模式
  - suite 级 `aggregateDiff`
  - suite 的 `fixturePreparation` 报告
  - 仓库内标准 suite 数据集和固定 donor-style fixture
  - manifest 驱动的 fixture 临时复制 + 固定时间戳
  - `25` 个 mixed-tool 标准 case 的仓库门禁
  - fixture manifest 的根目录级 / history 文件级覆盖守门
  - 标准 suite case 分类元数据
  - compare suite 的 case 元数据透传
  - compare / rollback 的按分类聚合视图
  - suite CLI 的 `--category` 过滤
  - suite CLI 的 `--expectation` 过滤
  - suite CLI 的 `--tool` 过滤
  - suite CLI 的 `--fixture` 过滤
  - suite CLI 的 `--tag` 过滤
  - suite CLI 的 `--tag-all` 过滤
  - suite CLI 的 `--exclude-tag` 过滤
  - suite CLI 的 `--exclude-fixture` 过滤
  - `categoryAggregate.fixtureAggregate` 二级分组
  - `--expectation` 的多值 OR、顺序无关归一化和重复值去重
  - rollback suite 的 recommendation/blocker 结构化原因汇总
  - compare suite 的 comparison/drift 结构化原因汇总
  - compare / rollback 文本模式的可读性回归
  - `DeepMemo` 多 topic 排序 / 多 agent / 多 fixture / `exclude_latest` / `current_topic_id` / alias / 复杂排序 / `agent-not-found` 标准覆盖
  - `TopicMemo` 多 agent / `agentId + maid` / `agent-not-found` / `no-settings -> 主人` 标准覆盖
- `LightMemo` 已支持：
  - `maid/folder` 硬过滤
  - donor 风格 `search_all_knowledge_bases`
  - `excluded folders`
  - `directory map`
- 当前全量回归已通过：`npm test = 76/76`
- 当前 `Phase C` 定向回归已通过：`16/16`
- 当前 Phase D CLI 定向回归已通过：
  - `active-memory-cli = 1/1`
  - `vcp-active-memory-cli = 5/5`
  - `compare-vcp-active-memory-cli = 14/14`
  - `rollback-active-memory-cli = 11/11`
  - `mainline-gate-cli = 2/2`
  - `http-observe-cli = 2/2`
  - `mainline-rollback-cli = 3/3`
- 当前 compare / rollback 联合定向回归也已通过：
  - `compare-vcp-active-memory-cli + rollback-active-memory-cli = 25/25`
- 当前全量回归最新基线：
  - `npm test = 123/123`
- 当前默认主链 gate 最新基线：
  - `npm run gate:mainline = passed`
  - `npm run gate:mainline:strict = passed`
- 当前 HTTP 运行态观察最新基线：
  - `npm run observe:http -- --json = passed`
  - 当前返回 `summary.status=warn`，原因是 watchdog 日志里能看到历史恢复记录
- 当前默认主链回滚预案最新基线：
  - `npm run rollback:mainline:plan -- --json = passed`
  - 当前返回 `summary.status=ok`
  - `rollbackTargetReachable=true`
  - `tools/list` 实探通过，返回 `record_memory / search_memory / memory_overview`
- 当前默认主链回滚演练最新基线：
  - `C:\Users\617\.codex\config.toml` 已临时切到 `6005` 并切回 `7605`
  - donor `initialize = 200`，返回 `Mcp-Session-Id`
  - donor `tools/list` 返回 `record_memory / search_memory / memory_overview`
  - 切回后 `npm run gate:mainline = passed`
- 当前扩展字段 drift 收口最新基线：
  - `npm run compare-active-memory -- --suite .\benchmarks\active-memory-suite\standard-suite.json --json --require-match = passed`
  - `matchedCaseCount = 34`
  - `extendedMismatchCountTotal = 0`
  - `driftReasonBreakdown = {}`
  - `npm run rollback-active-memory -- --suite .\benchmarks\active-memory-suite\standard-suite.json --json --require-ready = passed`
  - `readyCaseCount = 34`
  - rollback `extendedMismatchCountTotal = 0`
  - 当前复验记录：[phase-e-extended-drift-baseline-02.md](/A:/codex-memory/logs/phase-e-extended-drift-baseline-02.md)
- 当前排序 tie-breaker 收口最新基线：
  - `node --test .\tests\phase-c-active-recall.test.js = 21/21`
  - `npm test = 94/94`
- 当前错误语义标准 suite 门禁最新基线：
  - compare harness 已把 `query / rawQuery / blockedKeywords / inputSource` 收窄为 error-path 专属字段
  - 标准 suite 新增 `DeepMemo invalid-json` case，把错误输入诊断推进进 compare / rollback 门禁
  - 标准 suite 新增 `TopicMemo invalid-json` case，把 `TopicMemo` 的输入错误 donor 诊断也推进进 compare / rollback 门禁
  - 标准 suite 新增 `TopicMemo unknown-command` case，把显式错误指令 donor 语义也推进进 compare / rollback 门禁
  - 标准 suite 新增 `DeepMemo` 多关键词组合 `all-keywords-blocked` case，把 blocked/effective donor 诊断组合边界也推进进 compare / rollback 门禁
  - compare harness 现在会在新旧 success payload 都显式暴露 `meta` 时，额外比对 blocked/effective success-meta 字段
  - 标准 suite 新增 `DeepMemo` 多关键词组合“部分屏蔽但仍成功”case，把 success-path 的 blocked/effective donor 诊断也推进进 compare / rollback 门禁
  - 标准 suite 新增 `DeepMemo` 重复关键词去重 success case，把 blocked/effective 的去重与顺序稳定性也推进进 compare / rollback 门禁
  - 标准 suite 新增 `DeepMemo` 高级查询语法混用 success case，把短语 / 可选组 / 权重项混用时的 blocked/effective donor 语义也推进进 compare / rollback 门禁
  - 标准 suite 新增 `DeepMemo` blocked 配置重复值和大小写混用 success case，把 blocked config 归一化下的 blocked/effective donor 语义也推进进 compare / rollback 门禁
  - 标准 suite 新增 `TopicMemo history-read-error` fixture 与 case
  - `node --test .\tests\vcp-active-memory-cli.test.js = 17/17`
  - `node --test .\tests\compare-vcp-active-memory-cli.test.js = 14/14`
  - `node --test .\tests\rollback-active-memory-cli.test.js = 11/11`
  - `npm run compare-active-memory -- --suite .\benchmarks\active-memory-suite\standard-suite.json --json --require-match`
    - `matchedCaseCount = 34`
  - `extendedMismatchCountTotal = 0`
  - `npm run rollback-active-memory -- --suite .\benchmarks\active-memory-suite\standard-suite.json --json --require-ready`
    - `readyCaseCount = 34`
  - `extendedMismatchCountTotal = 0`
  - `npm test = 123/123`

## 恢复点

如果下一次要继续 `Phase D`，优先从这里接：

1. 检查 [STATUS.md](/A:/codex-memory/STATUS.md) 的阶段进度和待办。
2. 看 [README.md](/A:/codex-memory/README.md) 里的 active-memory CLI / compare harness 说明是否还和实现一致。
3. 如果要继续补 donor 对照，优先先看：
   - [active-memory-cli.test.js](/A:/codex-memory/tests/active-memory-cli.test.js)
   - [phase-c-active-recall.test.js](/A:/codex-memory/tests/phase-c-active-recall.test.js)
   - [vcp-active-memory-cli.test.js](/A:/codex-memory/tests/vcp-active-memory-cli.test.js)
   - [compare-vcp-active-memory-cli.test.js](/A:/codex-memory/tests/compare-vcp-active-memory-cli.test.js)
4. 如果要补配置或文档，优先同步：
   - `CODEX_MEMORY_ACTIVE_BLOCKED_KEYWORDS`
   - `CODEX_MEMORY_ACTIVE_RERANK_SEARCH`
5. 如果要继续补 suite 过滤，先以 `--expectation` 这条已经固化好的模式为模板，保持：
   - 文档参数说明
   - compare 回归
   - rollback 回归
   - 顺序归一化 / 去重 / 值域约束断言

## 下一轮最值钱的方向

- `Phase C` 可以进入维护性收尾，不必再默认继续扩 donor 细节。
- 继续扩 compare harness 的 donor 对照样本和字段 diff。
- active-memory CLI 现在已经能做 `health / rebuild / sync`，下一步可以考虑补更多管理型子命令或 overview 整合。
- rollback readiness 现在已经能给单条输入做只读判断，下一步可以考虑补批量样本和多 case 汇总。
- compare / rollback 现在都已经有仓库标准 suite；下一步更值钱的是继续扩 case 覆盖，而不是再回到临时样本。
- `Phase E / P1` 现在已经把 donor 风格错误语义推进进标准 suite 和 compare / rollback 门禁；后续如果继续做，更偏向细别名和样本扩容，而不是大块兼容缺口。
- `Phase E` 阶段总结已落盘到 `PHASE_E_SUMMARY.md`；当前阶段结论是：`P0` 主线能力已可日常使用，`P1` 高价值 donor 精修已收住主体，后续主要转入 backlog 驱动的增量精修。
- 阶段导航页已落盘到 `PHASE_NAVIGATION.md`；后续恢复上下文时，优先从导航页进入，再跳 `STATUS -> PHASE_E_SUMMARY -> PHASE_E_BACKLOG`，可以减少在 README 中来回翻找。
- 项目正式收官说明已落盘到 `PROJECT_CLOSURE.md`；当前统一口径是：主项目已完成，后续工作转为维护与精修，不再把 donor 边角 polish 视为主项目 blocker。
- 如数据规模继续扩大，再把增量同步从“全树扫描”升级为更细的 agent/topic 快照策略。
- `Phase D` 已开始落地，但仍然坚持只改 `A:\codex-memory`，不在 donor 项目里补丁式开发。
