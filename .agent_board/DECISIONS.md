# DECISIONS.md — codex-memory

| ID | Context | Decision | Reason | Alternatives | Evidence / Validation | Date |
|---|---|---|---|---|---|---|
| CMD-0001 | Autopilot setup | Use A4-Sustained Local Autopilot with codex-memory-specific gates | Repository is mature and needs sustained local work without bypassing gates | Generic AGENTS only | README current reality + AGENTS.md | 2026-05-05 |
| CMD-0002 | Runtime default | Treat HTTP MCP 7605 as default mainline assumption, but verify before relying | README recommends HTTP MCP for Codex Desktop stability | stdio default | observe:http / health when needed | 2026-05-05 |
| CMD-0003 | Rollback | Treat rollback planning as read-only; config changes require approval | Prevent accidental mainline switch | auto apply patch | rollback:mainline:plan only | 2026-05-05 |
| CMD-0004 | Remote boundary | Push remains explicit-approval only | Autopilot increases local momentum but must not create remote side effects silently | auto-push | user approval before each push | 2026-05-05 |
| CMD-0005 | DeepMemo diagnostics | Error meta query fields should follow the same `keyword` / `key_word` / `KeyWord` / `query` alias precedence as search input normalization | Donor-compatible diagnostics should not lose the query when an alias form reaches an error path | Leave alias support only on success/search path | `deepmemo-missing-maid-keyword-alias`; `deepmemo-agent-not-found-keyword-alias`; compare `39/39`; rollback `39/39`; `npm test 123/123` | 2026-05-05 |
| CMD-0006 | Claude MCP client scope | Prefer Claude Code local HTTP MCP as the first Claude acceptance path; treat Claude Desktop remote connectors and DXT packaging as separate work | Current `codex-memory` mainline already exposes streamable HTTP at `127.0.0.1:7605`; remote connectors originate from Anthropic cloud and are not the local-loopback path | Start with Desktop DXT or stdio | Official Claude Code MCP docs + local health preflight | 2026-05-06 |
| CMD-0007 | Claude Code model-side validation | Use `deepseek-ai/deepseek-v4-flash` for current Claude Code model-side acceptance unless the user specifies another model | User approved the model switch; Flash completed model smoke, read-only capability assessment, and MCP `memory_overview` validation | Keep using historical `deepseek-v4-pro` | `claude -p ... --model deepseek-ai/deepseek-v4-flash`; `memory_overview 调用成功`; `npm run gate:mainline` | 2026-05-06 |
| CMD-0008 | Next-phase planning | Keep the next-phase plan thin and put executable tasks in `MAINTENANCE_BACKLOG.md` | Avoid adding another large, constantly drifting root document; docs governance should define fact-source responsibilities first | Import the downloaded long plan verbatim | `CODEX_MEMORY_NEXT_PHASE_PLAN.md`; `DOCS_GOVERNANCE.md`; `CMV-0043` | 2026-05-08 |
| CMD-0009 | `gate:ci` planning | Keep M-011 as fixture-only design only; do not add `package.json` scripts or `.github/workflows` until the sandboxed command contract is implemented and validated | Prevent CI from inheriting local daemon, provider, profile, or durable-state assumptions from `gate:mainline` | Add `gate:ci` script/workflow immediately | `GATE_CI_FIXTURE_ONLY_DESIGN.md`; M-011 docs boundary | 2026-05-08 |
| CMD-0010 | 4-Agent Compact Autopilot activation | Treat `4-Agent` as logical roles in one Commander-led control plane; temporary agents start read-only unless file locks and task contracts authorize writes | User asked to start the mode, but current dirty source worktree makes broad edits unsafe without verification | Spawn write-capable Workers immediately | `docs/SINGLE_WINDOW_4_AGENT_COMPACT_AUTOPILOT.md`; `git status -sb`; `FL-0003`; read-only role task contracts | 2026-05-12 |
| CMD-0011 | Scope metadata durability | Persist scope metadata in diary headers for rebuild durability, but strip those internal headers before chunk indexing and recall output | Scope needs to survive fresh shadow rebuilds without leaking raw workspace/task/conversation identifiers into user-facing recall content | Store scope only in SQLite; store diary headers without output stripping | `tests/scope-filter.test.js`; `ChunkIndexingService`; `CMV-0064`; strict gate | 2026-05-12 |
| CMD-0012 | Search scope semantics | Treat supplied `search_memory.scope` fields as filters regardless of `strict`; use `strict` only as audit/overview hard-isolation intent | This preserves privacy-by-default scoped recall and removes ambiguity from the MCP contract | Make filtering conditional on `strict`; ignore `strict:false` scope fields | `src/core/constants.js`; `tests/scope-filter.test.js`; `CMV-0065` | 2026-05-12 |
| CMD-0013 | Legacy scope-column compatibility | On shadow upsert, omitted scoped fields may remain nullable on new schemas but must use old NOT NULL column defaults when upgraded databases require them | Prevent accepted `record_memory` writes from degrading when older SQLite schemas still enforce scoped-column defaults | Force new hard-coded defaults for all schemas; run an automatic migration | `src/storage/SqliteShadowStore.js`; `tests/scope-filter.test.js`; `CMV-0066` | 2026-05-13 |
| CMD-0014 | Recall output sanitization | Treat `text` as user-facing recall output and strip internal diary scope markers from both fresh and legacy chunk candidates | Existing `snippet`/`content` were sanitized, but `text` could carry stale raw chunk headers from upgraded stores or cached candidates | Leave `text` raw for scorer/debug use | `src/recall/CandidateGenerator.js`; `src/recall/KnowledgeBaseRecallPipeline.js`; `tests/scope-filter.test.js`; `CMV-0067` | 2026-05-13 |
| CMD-0015 | Diary scope header parsing | Parse scope metadata only from the diary header block before `Content:` during rebuild | User-authored marker-like lines inside content must not silently rewrite project/workspace/client/visibility metadata after rebuild | Continue scanning the whole diary text; require escaping marker-like content lines | `src/storage/DiaryStore.js`; `tests/scope-filter.test.js`; `CMV-0068` | 2026-05-13 |
| CMD-0016 | Diary tag footer parsing | Parse rebuilt tags only from the actual footer tag capture after `Evidence:` | User-authored `Tag:` lines inside content must not silently rewrite record tags after rebuild | Continue scanning the whole diary text for the first `Tag:` line | `src/storage/DiaryStore.js`; `tests/scope-filter.test.js`; `CMV-0069` | 2026-05-13 |
| CMD-0017 | Scope backfill dry-run accounting | Count partial scope records by missing field, and count `wouldUpdate` when a recommended-default field is missing | Operators need dry-run evidence for records that have one scope field but still lack client/project/visibility completeness | Treat any scope field as already complete | `src/cli/scope-backfill-dry-run.js`; `tests/scope-backfill-dry-run.test.js`; `CMV-0070` | 2026-05-13 |
| CMD-0018 | Marker stripping boundary | Require a real `Content:` boundary before stripping internal diary marker lines | `Record-Type:` can appear in ordinary content; without a diary boundary, stripping mutates user-authored text and recall output | Treat any `Record-Type:` line as diary-like | `src/storage/DiaryStore.js`; `tests/scope-filter.test.js`; `CMV-0071` | 2026-05-13 |
