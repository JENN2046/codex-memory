{
  "version": "v1",
  "description": "Fixture-only workflow query suite for codex-memory VCP Memory Core acceptance. Cases are sanitized and do not call providers or mutate data.",
  "fixture": "../default-dataset.json",
  "cases": [
    {
      "id": "rq-001",
      "area": "observability",
      "query": "How can I inspect audit logs and recall health for Codex memory?",
      "target": "memory_overview",
      "expected": {
        "mustContain": [
          "memory overview",
          "audit logs",
          "recall activity"
        ],
        "mustNotContain": [
          "provider smoke"
        ]
      },
      "source": "benchmarks/default-dataset.json#memory_overview",
      "notes": "fixture-only query derived from sanitized benchmark document memory_overview"
    },
    {
      "id": "rq-002",
      "area": "storage",
      "query": "What happens if the side index write fails after the diary entry is already saved?",
      "target": "dual_write_shadow",
      "expected": {
        "mustContain": [
          "dual-writes",
          "SQLite shadow storage",
          "reconcile task"
        ],
        "mustNotContain": [
          "provider smoke"
        ]
      },
      "source": "benchmarks/default-dataset.json#dual_write_shadow",
      "notes": "fixture-only query derived from sanitized benchmark document dual_write_shadow"
    },
    {
      "id": "rq-003",
      "area": "recall",
      "query": "Which component reuses ranked retrieval candidates until the knowledge base changes?",
      "target": "candidate_cache",
      "expected": {
        "mustContain": [
          "candidate cache",
          "ranked chunk candidates",
          "knowledge base sync"
        ],
        "mustNotContain": [
          "provider smoke"
        ]
      },
      "source": "benchmarks/default-dataset.json#candidate_cache",
      "notes": "fixture-only query derived from sanitized benchmark document candidate_cache"
    },
    {
      "id": "rq-004",
      "area": "recall",
      "query": "How does retrieval inject recent conversation state into the query embedding?",
      "target": "context_vector",
      "expected": {
        "mustContain": [
          "context vectors",
          "recent conversation history",
          "search query representation"
        ],
        "mustNotContain": [
          "cleanup legacy chunks"
        ]
      },
      "source": "benchmarks/default-dataset.json#context_vector",
      "notes": "fixture-only query derived from sanitized benchmark document context_vector"
    },
    {
      "id": "rq-005",
      "area": "provider-profile",
      "query": "Which command checks remote provider connectivity and prints a report?",
      "target": "provider_smoke",
      "expected": {
        "mustContain": [
          "provider smoke CLI",
          "remote embedding",
          "JSON or text report"
        ],
        "mustNotContain": [
          "memory overview"
        ]
      },
      "source": "benchmarks/default-dataset.json#provider_smoke",
      "notes": "fixture-only query derived from sanitized benchmark document provider_smoke; no provider call is made"
    }
  ]
}
